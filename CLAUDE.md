# Project Instructions

## Building

Always use `make build` to rebuild the digest binary. Never run `go build` directly.

## Testing

Always use `make test` to run tests. Never run `go test` directly.
